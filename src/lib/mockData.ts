import { Person, Request, Papel, TipoAusencia, Status } from "./types";

export const mockUsers: Person[] = [
  // Mock users removed - using real database data now
];

export const mockRequests: Request[] = [
  // Mock requests removed - using real database data now
];

// Current user mock - now handled by authentication
export const currentUser: Person | null = null;